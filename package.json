{
  "name": "git-archaeologist",
  "version": "0.0.1",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
